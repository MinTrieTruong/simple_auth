# Simple Auth – Lab 6


Ứng dụng minh họa xác thực người dùng đơn giản bằng **Basic Auth** và **Cookie/Session** trong Node.js.  
Đã thêm MSSV và Họ tên hiển thị trên tất cả các trang.

## Cài đặt & Chạy
```bash
npm install
node basic_auth.js        # kiểm tra Basic Auth
node cookie_auth.js       # kiểm tra Cookie + Session


---

### Commit lên GitHub
```bash
git add README.md
git commit -m "Update README with inline screenshots"
git push origin main
